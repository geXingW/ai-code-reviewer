# ai-code-reviewer backend — 生产依赖
# 安装：pip install -r requirements.txt

# Web 框架 & 服务器
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 数据库 & ORM
# <2.1 上限：SQLAlchemy 2.1.0 变更了 select() 的类型推断（Select[tuple[Entity]] ->
# Select[Entity]），导致 mypy 全量报错；2.1 运行时行为也未经本项目验证，适配作为
# 独立任务处理。详见 PR fix/backend-mypy。
sqlalchemy[asyncio]>=2.0.0,<2.1.0
asyncpg>=0.29.0
aiomysql>=0.2.0
pymysql>=1.1.0

# 数据校验 & 配置
pydantic>=2.8.0,<3.0.0
pydantic-settings>=2.4.0

# 加密
cryptography>=42.0.0

# 密码哈希（RBAC）
bcrypt>=4.0.0

# HTTP 客户端
httpx>=0.27.0

# Token 计数
tiktoken>=0.7.0

# 表单处理
python-multipart>=0.0.9

# JWT
PyJWT>=2.8.0
