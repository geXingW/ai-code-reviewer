"""LLM-direct review engine (placeholder for Issue #6).

The real prompt orchestration, sliding-window line localisation, and
second-pass filtering land in Issue #6. This package only ships the
registry-discoverable skeleton so the abstraction layer is exercisable
end-to-end.
"""
