version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: ci
      include: scope

  # Will be enabled after Issue #2 introduces backend/pyproject.toml
  # - package-ecosystem: pip
  #   directory: /backend
  #   schedule:
  #     interval: weekly

  # Will be enabled after Issue #11 introduces frontend/package.json
  # - package-ecosystem: npm
  #   directory: /frontend
  #   schedule:
  #     interval: weekly
