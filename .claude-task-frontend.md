请严格按照 /home/ubuntu/ai-code-reviewer/docs/specs/commit-review-per-project-config.md 中的 spec 执行前端部分。

阶段2：TypeScript 类型 → ProjectDialog 表单 → App.tsx 同步

要点：
1. api.ts 中 ProjectConfig、ProjectFormPayload、ProjectUpdatePayload 三个类型都要加新字段
2. ProjectDialog.tsx 的 initialEmptyForm 加默认值，编辑模式回填，UI 加 commit 审查配置卡片
3. App.tsx 中 initialProjectForm 同步更新
4. 完成后运行：npm run build && npm test