请严格按照 /home/ubuntu/ai-code-reviewer/docs/specs/commit-review-per-project-config.md 中的 spec 执行后端部分。

阶段1：DB 迁移 → Model → Schema → Webhook/Orchestrator 逻辑修改 → 测试

要点：
1. Alembic 迁移文件命名：0012_project_commit_review.py，down_revision='0011_commit_reviews'
2. 所有字段默认值：commit_review_enabled=False, commit_review_max_per_push=10
3. Webhook 层的全局 settings.commit_review_enabled 作为第一道防线保留，project 级作为第二道
4. 单测中构造 Project 对象时需带上新字段
5. 完成后运行：ruff check backend/ && mypy backend/app && pytest backend/tests/ -x -q